package com.smartjam.smartjamapi.enums;

/**
 * Supported client device types. Used to differentiate notification delivery strategies(in future. Right it's not such
 * useful =)).
 */
public enum DeviceType {
    ANDROID,
    IOS,
    WEB
}
